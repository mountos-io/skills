{
  "name": "mountos",
  "owner": {
    "name": "mountOS",
    "url": "https://mountos.io"
  },
  "metadata": {
    "description": "Agent skills for running mountOS, self-hosted POSIX-compatible distributed storage.",
    "version": "1.0.0"
  },
  "plugins": [
    {
      "name": "deploy",
      "source": "./deploy",
      "description": "Deploy, verify, and operate a self-hosted mountOS storage system. Architecture diagrams, the ordered bring-up, per-stage verification, and the failure modes that report healthy while the system is broken."
    },
    {
      "name": "integrate",
      "source": "./integrate",
      "description": "Add mountOS storage under a product that already has customers, users, and an identity provider. Mapping an existing user base, credential-issuing shapes, the reconciliation loop, and picking a data surface per workload."
    },
    {
      "name": "troubleshoot",
      "source": "./troubleshoot",
      "description": "Diagnose a mountOS deployment that is failing or behaving wrongly: what evidence to gather in what order, which checks prove nothing, and the failure modes that report healthy while broken."
    },
    {
      "name": "conformance",
      "source": "./conformance",
      "description": "Run filesystem conformance and data-integrity suites (pjdfstest, LTP, fsx) against a mounted mountOS volume on Linux, and interpret the results."
    },
    {
      "name": "smallfiles",
      "source": "./smallfiles",
      "description": "Measure small-file workload speed (git clone, npm install) on a mounted mountOS volume. Speed, not correctness; use the conformance skill for verdicts."
    }
  ]
}
